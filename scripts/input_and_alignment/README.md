# Input and alignment workflows

Reusable input preparation, reconstruction, camera, projection, CRS, datum,
rendering, and export drivers. TUM2Twin RV1 orchestration has its own child
directory because it is a multi-step workflow.

