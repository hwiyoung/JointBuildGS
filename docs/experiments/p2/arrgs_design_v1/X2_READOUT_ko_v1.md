# X2 판독 — 프로파일 3동 (변화 B173 / 구멍 B036 / 정상 B022)

- 상태: TECHNICAL READOUT, 비확정적. `scientific_verdict: null`
- 체인: v6 (X1 판독의 진단 사다리 최종). 평가: `arrgs_v1/evaluation/rows.csv`
- 뷰어: 8884 — X2 런의 S5 증거 착색·S3+4 타임라인

## 1. 정량 (gt=e1, f1@0.5 / comp@0.25 / acc_med / z-spread)

| 건물 | E2 | E8 | ARRGS v6 |
|---|---|---|---|
| B022 정상 | 0.933/0.940/0.10/0.21 | 0.936/0.942/0.10/0.22 | 0.349/0.311/1.48/7.67 |
| B173 변화 | 0.907/0.817/0.10/0.15 | 0.882/0.915/0.11/0.77 | 0.385/0.593/2.20/7.63 |
| B036 구멍 | 0.502/0.307/0.08/0.09 | 0.541/0.341/0.08/0.09 | 0.203/0.066/3.41/3.91 |

런 간 변동 주의: B022 단독 재실행에서 f1 0.394를 기록(±0.05 수준 재현성) —
v1 품질 대역에서는 시드·뷰 샘플링 변동이 커서 소수점 둘째 자리 비교는 무의미.

## 2. 사전 등록 예측 대비 판정

- **P-X2-1 (변화 건물의 stale prior 기각)**: **시험 불성립(설계 전제 오류 판명).**
  B173의 "변화"는 기존 LoD2↔현재의 불일치이고, prior 소스인 2022 ALS는 이미
  현재와 같은 평지붕(RANSAC 결과 |n_z| 0.95~1.0) — A2 정합 게이트의 "ALS↔현재
  변화 희소" 실측과 정합. **D-2(ALS 한정) 결정의 필연적 귀결로, 이 데이터셋에는
  자연 발생 stale-ALS 시험대가 사실상 없다.** 통제 시험은 X3 δ-주입이 담당.
  부분 관찰: 증거 레이어는 작동 — prior 면 지지도 0.32/0.43 vs MVS 면 0.82~0.94로
  분화(v1 체인 실측), 지지도 낮은 면의 "재현" 표시 메커니즘 자체는 성립.
- **P-X2-2 (구멍 충전 + 정직 표시)**: **기각(v1)** — B036 comp 0.066으로 채움
  실패. 파편화(S5 roof 6면/ground 137면)가 지배. B036은 프로그램 전체에서 가장
  병리적인 건물(MVS 붕괴 + SMRF 오분류 이력)로, v1 표현력·마스크 한계가 집중
  타격. E9의 "분류-구멍 7동" 계열 처리로는 union이 이미 우수(E8 0.541).
- **P-X2-3 (정상 대조 재현)**: X1과 동일 대역 재현(0.35~0.39).

## 3. 해석

프로파일 시험의 원래 목적(중재·채움·대조)은 v1 품질 부족으로 판별력이 제한됨.
확보된 것: ① 증거 레이어(면별 지지도)의 분화 신호 ② "자연 변화 시험대 부재"라는
데이터셋 사실(향후 설계에 중요 — stale prior 시험은 δ-주입 통제 실험이 유일 경로)
③ 병리 건물(B036)에서 배열 파편화라는 구체적 v2 과제.
