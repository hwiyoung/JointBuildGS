# E9 판독 — conflict-aware 선택 결합 파일럿 (규칙 2종 × δ 3점, 확정 93동) v1

- task_id: `P2-JOURNAL1-E9-v1` · **DEVELOPMENT_NON_CONFIRMATORY** · `scientific_verdict: null`
- 사전 등록: `configs/p2/journal1_e9_v1/e9_union_v1.json` (규칙·예측·반증 조건)
- 산출물: `phase-payloads/p2/journal1_e9_v1/P2-JOURNAL1-E9-v1/{diagnosis,runs/*}`

## 1. 레버 1 진단 — 분류-구멍은 집중형

hole = raw−cls6 completeness@0.25 (동일 footprint-only 크롭 내부 비교):
**≥0.10은 93동 중 7동뿐**(중앙값 0.001, p90 0.043) — 4906982(0.35↔0.97)는 극단
예외. 상위 7: 4906982·4906975·42364661·4907519·4906984·104586480·42364659
(마지막은 union-회귀 건물과 겹침). → prior-유도 분류 수리는 7동 국소 레버.

## 2. 레버 2 — 선택 결합의 트레이드오프 면 (gt=e1 중앙값, 확정 93)

| 규칙 | δ=0 (f1 / comp / z-spread / Roofer93) | δ=1.0 (f1 / comp / z) | δ 거동 |
|---|---|---|---|
| **E8** 전부 합침 | 0.952 / 0.983 / 0.201 / 78 | 0.942 / 0.977 / 0.238 | 오염 노출 (z +18%) |
| **E9v1** 순수 채움만(겹침 전부 폐기) | 0.935 / 0.958 / **0.187** / **80** | 0.935 / 0.954 / **0.187** | **완전 플랫** — 회귀 5/7 복구, 그러나 채움 이득 20%만 보존(반증 조건 발동) |
| **E9B** 중복만 폐기(수직-분리 유지) | **0.951 / 0.983** / 0.198 / 78 | 0.940 / 0.976 / 0.226 | 이득 회복, 그러나 **E8과 동급으로 오염** |

selection 실측(δ=0): ALS 491만 중 순수 채움 196만 / 중복-일치 168만 /
**수직-분리 127만(겹침의 43%)**. δ가 커지면 오염점이 수직-분리 클래스로
편입되어 E9B가 그대로 보존 — E9B(δ) 취약성의 원인.

## 3. 결론 — 기하 규칙 계열에는 좋은 점이 없다 (판정 함수의 실험적 필요)

세 규칙이 그리는 경계면: **채움 이득과 오염 강건을 기하 신호만으로 동시에
가질 수 없다.** 모든 모호성이 **수직-분리 클래스**(E2는 지면·다른 표면을 보고
ALS는 그 위를 본 127만 점)에 응집한다 — 이 클래스는 (a) MVS 실패 위의 진짜
채움이거나 (b) stale/오염이며, C2C·깊이 잔차 등 3D-대-3D 신호는 정의상 이곳에
비교 대상이 없어 (a)/(b)를 구분할 수 없다(두 경우 모두 "충돌"로만 보임).

그 자리에 남은 현재 증거는 **영상**이다(MVS는 실패했어도 픽셀은 그 자리를
본다). 가설 표면(구 지붕)을 현재 카메라로 렌더해 실루엣·에지·가림과 대조하면
(a) 지지 / (b) 모순 / 가림-판단불가의 3갈래 판정이 가능하다 — **미분 가능
렌더링이 E9의 유일한 미결정 파라미터를 채우는 판정 함수**가 되는 실험적
근거. → HV-3은 "δ 탐지 일반전"이 아니라 **"수직-분리 클래스 판정전"**으로
조준한다(통제전: δ-주입 채움의 에지 정합; 실전: A-티어 구지붕 렌더의 영상
지지도, C-티어 대조; 반증: 값싼 신호가 동급이면 GS는 외관만 남는다고 결론).

## 4. 재현

```bash
bash scripts/p2/journal1_e9_v1/e9_run_host.sh <ARTIFACT_ROOT>   # 6런 멱등
# 진단: python -m scripts.p2.journal1_e9_v1.e9_diag_classification_holes
```
