# P2 실험 진행 현황

- 마지막 결과 커밋: `54508da0dda633840e6ee31db585adbf4fbb4cba`
- writer: Work Host
- scientific_verdict: `null`

## 무엇을 확인하려는 실험인가

같은 건물에 대해 C1 UAS LiDAR, C2 영상 MVS, C3 image-only GS를 각각 Roofer로
LoD2 모델로 만든 뒤, 어느 단계에서 몇 동이 실패하는지 비교한다.

## 왜 51동인가

전체 연구대상은 199동이다. 그중 독립 UAS 지붕 관측과 pilot 입력 지지가 있는
진단 후보 72동을 결과를 보기 전에 정했고, 이번에는 development 51동만 사용했다.
validation 11동과 held-out 10동은 열지 않았다. 이 72동은 strict Gate `E_paired`와는
다른 development용 진단 roster다.

정성 그림 5동은 development에 존재하는 5개 공간 group에서 각각 한 동을 hash로
사전선정했다. 성공하거나 실패한 결과를 보고 고른 사례가 아니다.

## 무엇을 넣어 어떻게 처리했나

| 조건 | 입력 | 처리 | 실제 출력 단위 |
|---|---|---|---:|
| C1 | current UAS LiDAR | class 2/6 점군 → Roofer | AOI 전체 CityJSONSeq 1개 |
| C2 | 937-view common-base dense MVS | GS 없이 Roofer | component 6개 |
| C3 | 같은 937 images/poses·MVS·semantic | GS 30k → surface → Roofer | surface 202개, Roofer 작업 18개 |

## 무엇이 나왔나

| 조건 | 건물 | G0 생성 | G1 형식 | G2 위상 | G3 후보 | G4 후보 | 공식 PASS |
|---|---:|---:|---:|---:|---:|---:|---:|
| C1 | 51 | 51* | 51* | 51* | 0 | 2 | `null` |
| C2 | 51 | 50 | 50 | 50 | 0 | 6 | `null` |
| C3 | 51 | 35** | 37** | 19 | 0 | 1 | `null` |

`*` C1의 51은 독립 모델 51개가 아니다. 51동이 같은 CityJSONSeq 한 개를 공유하며,
그 단일 파일을 val3dity로 검사한 결과 G2가 PASS했다.

`**` C3에서 건물과 component가 정확히 1:1로 연결된 것은 6동뿐이다. 나머지는 shared,
multiple 또는 unassociated다.

G3의 0은 현재 matcher가 큰 Roofer 면과 작은 UAS roof patch를 대응시키지 못한 결과라
공식 실패 수가 아니다. G4는 임시 1 m/2 m 기준의 진단값이다. 따라서 공식 G3/G4와
`PASS_usable`은 아직 모두 `null`이다.

## C1을 reference로 믿을 수 있나

현재 상태로는 아니다.

- 장점: current UAS LiDAR라는 고품질 센서 입력을 사용했고 결과 파일과 위상은 유효하다.
- 문제: C1 input과 평가용 UAS geometry가 같은 계보라 독립 정확도 평가가 아니다.
- 더 큰 문제: 51동을 한 Roofer output으로 만들었다. C1 height MAE 중앙값은 `5.22 m`,
  RMSZ 중앙값은 `5.81 m`이고 임시 G4 통과는 `2/51`뿐이다.

즉 UAS 데이터 자체가 나쁘다는 뜻이 아니라, 현재 C1의 건물 분리와 Roofer read-out이
상한 baseline 역할을 하지 못한다는 뜻이다. C1은 당분간 `self-reference diagnostic`으로
두고, 건물별 Stage 3를 고친 뒤 다시 확인해야 한다.

## 그림은 무엇을 보면 되나

점은 평가용 UAS roof cell, 빨간 면은 실제 Roofer RoofSurface다. 빨간 면이 한 건물보다
훨씬 넓거나 여러 부채꼴로 갈라지거나 비어 있으면 건물 분리/read-out 실패다.

- 로컬 폴더: `C:\Users\Hwiyoung\ChatGPTWork\Projects\JointBuildGS-artifacts\p2\c3_development_evaluation_candidate_v1`
- `4906981`: C1/C2/C3 면이 건물보다 넓게 연결됨
- `4906982`: C3 다중 roof fan
- `4959314`: C3 unassociated
- `4959327`: C3 1:1 후보지만 높이 기준 실패
- `4959461`: C3 multi-component

자세하지만 같은 순서로 읽을 수 있는 보고서는
[C1–C3 입력·처리·결과 설명서](c1_baseline_audit_v1/TECHNICAL_RETURN_REPORT_v1.md)다.

## 다음 한 작업

C1–C3에 공통으로 적용할 건물별 Stage 3 분리 규칙과 G3 matcher를 먼저 고친다.
새 GS 학습이나 C4/C5보다 이 문제가 앞선다. 기존 결과는 재계산하지 않고 재사용한다.
