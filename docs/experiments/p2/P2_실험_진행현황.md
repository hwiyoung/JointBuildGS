# P2 실험 진행 현황

- 마지막 확인일: 2026-08-03
- 마지막 Experiment Host 결과 커밋: `3396783b766be6edfc03d1c2f3da762f4e1a2dbb`
- 현재 writer: Work Host
- 과학적 판정: `null`

## 한 줄 현황

동일한 development 51동에서 C1/C2/C3의 실제 Stage 3와 단계별 153행 평가, 사전선정
5동의 비교 PNG까지 완료했다. C2는 생성·형식·위상은 50/51이지만 후보 높이 기준은
6/51, C3는 건물별 component 연결이 가장 큰 병목이며 후보 높이 기준은 1/51이다.
공식 최종 성공 수는 G3 matcher와 G4 threshold 동결 전이므로 아직 `null`이다.

## 지금까지 실제로 실행된 것

| 순서 | 작업 | 상태 | 결과 |
|---:|---|---|---|
| 1 | 공통 영상/pose와 입력 동결 | 완료 | 962/937/25, dense MVS 포함 |
| 2 | C1/C2 development 51동 | 완료 | 각 51행 |
| 3 | C3 semantic 및 전체 AOI GS 학습 | 완료 | semantic 937/937, seed 0, 30,000회 |
| 4 | C3 표면 추출과 Stage 3 Roofer | 완료 | 202 component, 18 unique Roofer 작업, 16 CityJSON read |
| 5 | 동일 51동 C1/C2/C3 단계별 비교 | 완료 | 153행과 정성 PNG 5개 |
| 6 | G3/G4 공식 기준 동결과 PASS 판정 | 미완료 | 후보값만 있으며 공식 PASS는 `null` |
| 7 | C4/C5 및 validation/held-out | 미실행 | 접근하지 않음 |

## 연구계약 04에 대한 현재 위치

| 계약 단계 | 의미 | 현재 상태 |
|---|---|---|
| `G_native` | 조건 자체의 원래 3D 표현 | C1/C2 입력과 C3 30k checkpoint 완료 |
| `S_extracted` | 원래 표현에서 표면 추출 | C3 202 component 완료 |
| `P_Roofer` | Roofer 입력 변환 | C3 200 eligible component, 18 unique 작업으로 중복 방지 |
| `H_LoD2` | Roofer 건물 모델 | 18 결과 중 16개에 CityJSON geometry |
| `G0–G2` | 생성·형식·3D 유효성 | 아래 표와 153행 결과 완료 |
| `G3–G4` | 지붕구조와 위치·높이 | 후보 진단 완료, 공식 기준 미동결 |
| `PASS_usable` | G0–G4 교집합 | 공식 값 `null` |

## 단계별 정량 결과

| 조건 | 분모 | G0 생성 | G1 형식 | G2 위상 | G3 후보 | G4 후보 | 공식 PASS |
|---|---:|---:|---:|---:|---:|---:|---:|
| C1 current UAS LiDAR upper | 51 | 51 | 51 | 범위 밖 | 독립평가 불가 | 독립평가 불가 | `null` |
| C2 current-image MVS | 51 | 50 | 50 | 50 | 0 | 6 | `null` |
| C3 image-only GS | 51 | 35* | 37* | 19 | 0 | 1 | `null` |

`*` C3 35/37은 평가 component 관점이다. 엄격한 건물↔component 1:1 연결은 6동뿐이며,
그 6동에서는 G0 5, G1 6이다. 다른 45동은 shared, multiple 또는 unassociated라 건물
단위 G0/G1을 `null`로 격리했다.

C3 association 분포는 다음과 같다.

- 정확한 1:1: 6동
- component 하나를 여러 건물이 공유: 13동
- 한 건물에 여러 component: 6동
- 공유와 다중이 동시에 존재: 12동
- 연결 없음: 14동

따라서 현재 C3의 첫 번째 병목은 loss나 학습 반복 수가 아니라 **전체 AOI GS surface를
건물별 Roofer 입력으로 제대로 분리하는 것**이다. 정성판에서도 C3의 큰 부채꼴 면,
다중 component, 미연결이 보인다.

G3 후보가 C2/C3 모두 0인 것은 현 matcher가 Roofer의 큰/concave 면과 UAS의 작은
지붕 plane patch를 대응시키지 못해 accepted pair가 0이 됐기 때문이다. 아직 matcher가
검증되지 않았으므로 이를 실제 지붕구조 0동 성공으로 해석하지 않는다. G4 후보는 임시
1 m/2 m 기준에서 C2 6동, C3 1동이며 공식 기준은 아니다.

## 모집단을 혼동하지 않기

현재 저장소에는 목적이 다른 두 roster가 있다.

- `U_target=199`: 선택 AOI의 전체 연구 대상.
- `72`: 독립 UAS 관측과 pilot 입력 지지가 있는 **진단용 후보**. development 51,
  validation 11, held-out 10으로 나뉜다. 이번 결과는 그중 51동만 사용했다.
- strict Gate `E_paired=10`: Gate S0 freeze recovery가 동결한 별도 primary paired set.
  development 4, validation 3, held-out 3이다.

즉 72를 공식 `E_paired`로 부르면 안 된다. 이번 51동 결과는 C1–C3 설계·실패 분석용
development pilot이고, validation/held-out 또는 C1–C5 confirmatory 결론이 아니다.

199→72의 정량 이유와 통과/불통 사례는
[`UAS_199_TO_72_EXPLAINER_v1.md`](../../research/preregistration/gate_s0/uas_eligibility_explainer_v1/UAS_199_TO_72_EXPLAINER_v1.md)에 있다.

## 정성 결과를 보는 곳

점은 독립 UAS reference, 빨간 면은 Roofer RoofSurface다. 각 PNG의 위쪽은 평면,
아래쪽은 3D 보기다.

- 로컬 폴더: `C:\Users\Hwiyoung\ChatGPTWork\Projects\JointBuildGS-artifacts\p2\c3_development_evaluation_candidate_v1`
- 대표 1: `DEBY_LOD2_4906981_C1_C2_C3_roof_comparison_v1.png`
- 대표 2: `DEBY_LOD2_4906982_C1_C2_C3_roof_comparison_v1.png`
- 미연결 사례: `DEBY_LOD2_4959314_C1_C2_C3_roof_comparison_v1.png`
- 다중 면 사례: `DEBY_LOD2_4959327_C1_C2_C3_roof_comparison_v1.png`
- 다중 component 사례: `DEBY_LOD2_4959461_C1_C2_C3_roof_comparison_v1.png`

전체 해석은 [C1/C2/C3 단계별 평가 보고서](c3_development_evaluation_candidate_recovery_r2_v1/TECHNICAL_RETURN_REPORT_v1.md),
기계판독 표는 로컬 `three_condition_development_diagnostics_v1.jsonl`에 있다.

## 재실행 방지와 다음 결정

- 이번 복구에서 reconstruction 0회, Roofer 0회, G2 0회다.
- R1 15.7GB, Images.zip, OPF.zip 재해시 0회다.
- validation/held-out/C4/C5/Fusion W1/R_ext 접근 0회다.

다음 한 개의 bounded task는 새 학습이 아니다. 현재 0/51을 만든 G3 matcher를 실제
건물별 roof surface 관계에 맞게 검증하고 G4 threshold를 동결해, 기존 153행에서 공식
G3/G4/PASS를 다시 집계하는 일이다. 이 판단 전까지 `scientific_verdict`는 `null`이다.

## 진행 현황 확인

```powershell
powershell -ExecutionPolicy Bypass -File scripts/repository/show_experiment_host_progress.ps1
```

원격 writer 상태는 다음으로 확인한다.

```powershell
ssh innopam@192.168.10.203 "cd /media/innopam/InnoPAM-8TB/hwiyoung/code/JointBuildGS-operator && git log -1 --oneline && git status --short"
```
