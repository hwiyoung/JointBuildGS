# 원격프롬프트 — 생성축 1파 재개: 포즈 채택 발행 + 관문 A v2 등재 + §3 이후 그대로 (2026-07-25 밤 발주)

> 성격: run `20260724_fusion_w1`의 재개 발주. 관문 A는 v2로 개정·**통과 등재**(기존 진단 측정 재사용 — 재측정 불필요), 전역 SE(3)는 김휘영 재판정으로 **채택**(카메라 포즈만 이동). 이후는 원 발주문 §3~§10 그대로.
> 선행 문서(먼저 읽고, 두 신규 문서의 사본을 run 폴더에 커밋): ① `docs/원격프롬프트*생성축1파*레이저영상융합*20260724.md`(원 발주문) ② `docs/사전등록*관문A_v2·SE3채택재판정*20260725.md`(이번 사전등록·판정 정본) ③ `docs/W*관문A진단_20260725.md`.
> 실행 원칙 유지: **판정은 사람(김휘영)** — 에이전트는 측정·산출까지. 모든 단계 산출물은 정량 표 + 정성 그림 쌍. 실패·예외는 숨기지 말고 issues.md 기록 후 보고. 브랜치 `exp/fusion-w1` 그대로.

## R0. preflight — 원 §0 5핀 재확인 + 추가 3항 (통과 못 하면 그 자리에서 중단·보고)

1. 원 §0의 5핀을 그대로 재실행한다. 단 **핀 ④의 COLMAP 포즈 해시는 R1의 보정판 발행 후 신 해시로 갱신·기록**한다(구 해시=이전 값으로 병기). 신구 해시 차이를 preflight 불일치 BLOCKED로 오독하지 말 것 — 이 발주가 그 변경의 승인이다. 영상 937장 집계·ALS SHA·footprint·참조 GML은 불변이어야 한다.
2. 추가 확인: ⓐ 작업 트리 clean·HEAD가 진단 커밋(45bce93) 이후 ⓑ 사전등록 v2 문서 존재·SHA256 기록 ⓒ `coreg_lock2` 산출물 존재(`global_selection.json`·`block_selection.json`·fit receipts).
3. counters 확인: 이 시점까지 `learning_runs_started=0`, `readout_runs_started=0`, `roofer_runs_started=0`, `scoring_runs_started=0` 유지.

## R1. 채택 변환 적용 — 보정판 포즈 발행 (딱 1회)

- **원천**: `coreg_lock2` 전역 후보를 산출물 파일에서 직접 로드한다(수치 손입력 금지). 교차 확인값(불일치 시 중단·보고): 회전 `0.0080702577°`, 이동 `(+0.040581, +0.020840, −0.097784) m`, rank 6, 최종 대응 33,969.
- `block_selection.json`의 consumer guard(`choice=none`·"선택된 변환으로 소비 금지")는 **무단 소비 방지용**이며, 본 발주 + 사전등록 문서 §4가 그에 대한 명시적 인간 승인이다. 채택 manifest(frozen/adopted transform)를 새로 발행하고 근거 문서 경로·해시를 기록한다.
- **적용**: 전 937장 카메라 포즈에 동일 변환을 **1회** 적용 → 보정판 COLMAP 모델을 별도 경로에 발행(원본 모델 보존·신구 해시 기록). **ALS LAZ·영상 픽셀·참조 GML·footprint는 불변.** ζ 이중 적용 금지(기존 확정 ζ=45.7 m 유지). 변환 이중 적용 금지 — 포즈에 1회만, ALS를 또 옮기지 말 것.
- **검증(전부 통과해야 R2 진행)**:
  ① 왕복 검증 — 역변환 적용 시 원 포즈 복원(수치 오차 한계 기록).
  ② 진단 재현 — 진단과 동일한 스크립트·membership으로 동별 잔차를 보정판 프레임에서 재계산했을 때 `W_관문A진단_20260725.md`의 "후보 적용" 열이 재현되어야 한다: 대응 가능 132동·matched 중앙≤0.3 m `132/132`·동별 중앙의 중앙 `0.072367 m`·T5 전체 총 잔차 `0.004186 m`(허용 수치 오차를 사전 명기·초과 불일치 시 발행 철회·BLOCKED 보고).
  ③ config 확인 — arm A/B가 같은 보정판 포즈 해시를 참조.
- **산출**: 채택 manifest(변환값·원천 해시·신구 포즈 해시·검증 표) + issues.md 등재 2건: ㉮ `FUS-W1-COREGDIAG-002` 지지율 요구의 범주 오류 — 확정(근거=대응 불가 46동 층 분포 2/11/33, 사전등록 문서 §5 인용) ㉯ 전역 SE(3) 채택 재판정 기록 — lock2 채택 계약이 개선폭 미달로 동결하지 않았던 사실을 병기하고, 채택은 사전등록 문서 §4의 별도 판정임을 명시.

## R2. 관문 A v2 등재 (재측정 불필요)

- 사전등록 문서 §2~§3 그대로: 결과 슬롯(n*=40 · 대응 가능 132/178 · 조건부 matched 중앙≤0.3 m 132/132 · 핵심군 24/24 · 대응 불가 46동=표면 2/높이 11/윤곽 33)을 run 기록에 등재하고 **관문 A = PASS**를 기록한다. P90·censored P90·지지율은 보조 기록으로만.
- 정성 확인(판정 아님): 핵심군 전 동, 동당 1뷰 이상에 분류 ALS(지붕·지면)를 **보정판 포즈**로 투영한 오버레이 PNG 생성·저장. 에지 매칭·수치 게이트 없음 — 육안 기록용.

## R3. 이후 = 원 발주문 §3~§10 그대로

- §3 시드·감독 준비 + P0′ → §4 학습(arm A 시드+감독 / arm B 감독 제거·각 2런·30k·항별 손실 비중 로그 필수) → §5 점군화→분류→Roofer→채점(층화·P0′ 델타) → §6 산출물(파일명 고정) → §7 우선순위 큐·시간 규칙(스모크 1동 먼저·처리율 로그·06:30 컷·파국 중단 규칙) → §8 판정 눈금 1~4(잠금 그대로·판정은 아침 검수에서 김휘영) → §9 함정 → §10 보고.
- **변경 금지 목록**: 대상 명단(`w1_targets.csv`)·눈금 1~4·레시피·가중 시작값(depth 0.5·normal 0.05)·큐 순서·채점 스크립트. 이번에 바뀐 것은 오직 두 가지 — (a) 관문 문안 = v2, (b) 학습이 읽는 포즈 = 보정판.
- 감독 depth/normal 맵은 **보정판 포즈 기준**으로 래스터화한다. 구 포즈로 만든 캐시·중간 산출물 재사용 금지(캐시 무효화를 명시 확인하고 기록).

## R4. 함정 (원 §9 전부 유지 + 신규)

- 신규: **게이트 초과 구현 금지**(판정은 사전등록 문안 그대로·보조 통계는 기록만) · censored 값≠실측(표에 구분 열) · **변환 이중 적용 금지**(포즈 1회만) · **구 포즈 캐시 서빙 금지**.
- 기존 유지: VM 마운트 구판 서빙 · 대소문자 우회 · CUDA 배너 오독(연산 스모크 1회) · 한글 파일은 파일 도구로 · Roofer 분류 요건(2/6) · val3dity는 CityJSON만 · PSNR 랭킹 금지 · 개수≠명단(정본 CSV 조인·ID 손입력 금지) · readout 직렬 + RAM cgroup.

## R5. 아침 회신 보고 형식

원 §10 형식 그대로 + 맨 앞에 두 절 추가: ① R1 검증 표(왕복·진단 재현 대조·신구 포즈 해시) ② R2 등재 확인(관문 A v2 = PASS·오버레이 목록). 해석·판정 문장 금지 — 관찰까지(검수 세션에서 김휘영).
