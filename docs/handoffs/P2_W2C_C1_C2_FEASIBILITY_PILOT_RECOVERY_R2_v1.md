# Work-to-Codex Task Packet — P2 C1/C2 feasibility pilot recovery R2 v1

## Handoff metadata

- handoff_id: `P2-W2C-C1-C2-FEASIBILITY-PILOT-RECOVERY-R2-v1`
- task_id: `P2-C1-C2-FEASIBILITY-PILOT-RECOVERY-R2-v1`
- phase: `P2 / development baseline feasibility before C3 strategy freeze`
- direction: `Work→Codex`
- status: `DRAFT_NOT_EXECUTION_AUTHORITY`
- user_approval: `PENDING_ACTIVATION`
- packet_version: `v1`
- source_commit: `PENDING_EXACT_REVIEWED_COMMIT`
- target_branch: `main`
- research_charter_version: `C1C5_CANON_v2`
- decision_log_through: `DEC-P1-013`
- created_at: `2026-08-02`
- scientific_verdict: `null`

This DRAFT is not execution authority. Activation requires an exact reviewed source,
`status=APPROVED_FOR_EXECUTION`, `user_approval=APPROVED_FOR_EXECUTION`, and a new
immutable offered/accepted chain.

## Answer first

The prior recovery closed at `663d62b10b1a94fd670ce99d86c5a9a171794f0b`
before namespace creation, smoke or scientific input access. Its packet omitted the
exact `user_approval` line required by the protected wrapper. The tracked wrapper was
also mode `100644`, so direct invocation first stopped before entry. No C1/C2 result
or partial computation exists.

R2 repairs only those authority/executability defects in a new add-once identity. It
does not change the scientific input, 51-building roster, conditions, references,
metrics, representative cases, retry policy or cost caps.

## Frozen scientific contract

- Common current source remains exact `962 / 937 / 25`.
- `U_target=199`; independent quantitative evaluation candidate remains 72 buildings
  in nine groups. Only the exact 51-building, five-group development split is opened.
- Conditions are only `C1_L_upper` and `C2_MVS`; exact result surface is 102 rows.
- Original outcome-free representative selection remains byte-identical.
- C1 is reported separately as `SELF_REFERENCE_UPPER_BASELINE`.
- C2 uses common-base MVS for reconstruction and independent UAS reference only after
  geometry and `R_derived` are frozen.
- Validation 11, held-out 10, C3–C5, LoD1/LoD2, Fusion W1 and `R_ext` remain
  inaccessible. `scientific_verdict` remains `null`.
- G2 remains canonically null; G3, G4 and `PASS_usable` remain unavailable.

## R2 implementation contract

1. Use new task, handoff, result, Return and external namespace identities ending in
   `RECOVERY-R2-v1` / `recovery_r2_v1`. Do not reopen either closed predecessor.
2. The activated packet must contain both exact lines consumed by the wrapper:
   `status=APPROVED_FOR_EXECUTION` and
   `user_approval=APPROVED_FOR_EXECUTION`. A regression test must parse the actual
   activated packet with the same fail-closed authority parser.
3. Track the R2 host wrapper as executable (`100755`) and also document its exact
   `bash` invocation. Test the Git mode and shell syntax.
4. Container Git ownership for zero-scientific preflight must be explicit and
   read-only; a missing safe-directory setting must not cause another retry.
5. The new `100-accepted` must reuse the exact original non-nested closed attestation:
   source commit `896fe284bc4d496e6e9c79720f4e75396a41d0b2`, Git receipt-blob
   SHA-256 `705348ecde9d139254bdd24e59ed02312d5321c20f802649f1ce4ca19f5b9bda`,
   and five-record identity
   `f63d5d4405157615d807d6babd4a9bf74a16ab13818193945ed9bbfc02532db3`.
   Acceptance-time payload full-read/hash remains 0.
6. Machine decisions remain one reserved tab-delimited record and fail closed on
   missing, duplicate, malformed, action or attempt drift.
7. Existing recovery-v1 packet/Return/receipts and all earlier evidence are immutable.

## New add-once write owners

```text
artifacts/manifests/handoffs/P2-W2C-C1-C2-FEASIBILITY-PILOT-RECOVERY-R2-v1
artifacts/manifests/p2_baselines/c1_c2_feasibility_pilot_recovery_r2_v1
docs/experiments/p2/c1_c2_feasibility_pilot_recovery_r2_v1
docs/handoffs/returns/P2_C2W_C1_C2_FEASIBILITY_PILOT_RECOVERY_R2_RETURN_v1.md
```

External namespace:
`artifact://JointBuildGS/phase-payloads/p2-baselines/c1_c2_feasibility_pilot_recovery_r2_v1/P2-C1-C2-FEASIBILITY-PILOT-RECOVERY-R2-v1/`.

The offered receipt must protect both closed C1/C2 chains and their packets/Returns,
all Gate-S0 evidence, the R2 packet/config/runner/schema/validator/tests after offer,
the research canon, phase history and Fusion W1. The allowed-path validator is
authoritative for every other Git path.

## Execution lifecycle

1. Experiment Host fetches and reads the remote R2 packet/source/000 before a clean
   fast-forward-only pull.
2. Verify exact image
   `sha256:251f83c17879a83b0c3dda5b9d71cbf45ca72cc0fdcbc89994194dc3edb86774`
   and zero-scientific preflight before `100-accepted`.
3. Create/push/validate the exact reuse-based `100-accepted` without artifact root or
   payload hash, then run the executable committed wrapper once.

   ```bash
   bash scripts/p2_baselines/c1_c2_feasibility_pilot_v1/run_pilot_host.sh \
     /media/innopam/InnoPAM-8TB/hwiyoung/code/JointBuildGS-artifacts \
     sha256:251f83c17879a83b0c3dda5b9d71cbf45ca72cc0fdcbc89994194dc3edb86774 \
     PENDING_EXACT_REVIEWED_COMMIT \
     P2-C1-C2-FEASIBILITY-PILOT-RECOVERY-R2-RUN-v1
   ```

   Activation must replace the pending source token above with the exact reviewed
   `source_commit`; the wrapper command is invalid while this packet remains DRAFT.
4. Synthetic smoke must pass before scientific mounts. Process and digest each exact
   input only in its existing single stream.
5. Produce exactly 51×2 rows, finalize and promote only compact R2 outputs.
6. Independent reviewers check science/leakage, ownership/receipts and
   reproducibility/no-repeat before Return and `200`.
7. Finish with Return/result commit, separate artifact-inheriting `200`, and
   direct-child `300` returning writer ownership. Payload rereads at 200/300 are 0.

## Stop conditions

- authority, source, image, receipt, input, roster, reference or namespace mismatch;
- any validation/held-out/C3–C5/LoD1/LoD2/Fusion/`R_ext` access;
- repeated scientific source stream, duplicate Roofer calculation or old namespace
  reuse;
- ambiguous machine channel, incomplete 102-row surface or cost-cap breach.

On a stop condition, do not rerun or salvage. Return evidence, close the chain, return
writer ownership and keep `scientific_verdict: null`.

## Work Host post-return deliverables

After successful C1/C2 return, Work Host will independently produce:

1. a concise UAS-reference explainer from the existing frozen attrition/eligibility
   ledgers, including the `199→129→124→96→94→89→72` progression and pass/fail examples;
2. a C3 training-strategy DRAFT grounded in C1/C2 failure modes, without executing C3.
