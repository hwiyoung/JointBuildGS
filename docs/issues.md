# issues.md — 무인 런 실패·스킵 로그

> 무인 드라이버(run_overnight.sh)가 항목 실패 시 여기에 타임스탬프로 append. 아침에 검토(판정=김휘영).
> (셋업 중 d12_buckets 경로 버그로 1회 "p0c_verdict missing"가 기록됐으나 경로 수정 후 정상 — 무시. 실제 런 buckets OK·all64=64.)
- 2026-07-09 corrected-S1 w=100 full runs: distortion tail share median 2.97-3.44%, below preregistered rough 5-15% target; scene_scale_sq/prune repair still executed and evaluated as observation material, not verdict.
- 2026-07-09 corrected-S1 recheck Step 5-B: local Omnidata/DSINE mono-normal runtime and model weights not found; COLMAP/PatchMatch normals were not used as a substitute because the requested check is mono-normal. Logged as observation limitation, not verdict.
- 2026-07-09 S1 full factor B-1: first two long train wrapper logs missing after detached tool sessions; final checkpoints and audit CSVs present, fingerprint marks final_ckpt_present_log_missing (results/tum_transfer/e5_s1_full_factor/C001/train_logs)
- 2026-07-10 S2 evaluate: Docker image lacks `ogr2ogr`; evaluation initially stopped at footprint GPKG conversion. Recovered by using the existing `w2_city3d/footprints_scene_aoi.geojson` cache, then reran evaluation in Docker.
- 2026-07-10 S2 B-2: initial pipeline-strip render failed because cache defaulted to `/.cache`; recovered by setting writable cache defaults and rerunning strips with 0 final strip issues.
- 2026-07-10 S2p A-1': Depth Anything V2 Large 14-building multi-view roof median was 8.10335 m; the locked `>8 m` branch skipped the Pearson gate and both Arm 3 runs. This is a preregistered branch outcome, not a runtime failure.
- 2026-07-10 S2p provenance QA: selected r2 readout initially rewrote `readout_fingerprints.csv` to one row, and the generic evaluator replaced the task manifest with an output-only manifest. Recovered by a full `skip_existing` readout fingerprint rebuild and a merged-manifest restore in the task harness; final QA passed 2 train/readout runs and 21 figures.
- 2026-07-13 S3 Track 0 preflight: the active C001 `semantic/` masks are crops of `clean_labels_geoidfix`, generated by raycasting the same reference LoD2 GML used by T0-1; no active C001 FM-mask output with fixed model/weight provenance was found. T0-1 would therefore be a circular self-consistency check rather than the locked FM-label gate. T0-1, the S3 loss gate, and all Track A training were not started pending the intended FM label path or generator provenance.
- 2026-07-13 S3-A redefinition: Kim Hwiyoung accepted the provenance stop and re-scoped this wave to an oracle-label mechanism upper-bound test. T0-1 is now a non-gating self-consistency reference; Track A requires completed T0-2 plus a passing T0-4 gate. No S3 training had run before this redefinition.
- 2026-07-13 S3-A footprint-split smoke BLOCKED: the locked class mask has no instance id, so the +20 m footprint projection needs a non-GT projection height. A 3-view C00118 smoke using Arm 1-prime zero-iteration input-point median z passed the cache/dtype/7+7 cutline/partition contracts, but the rule could not both retain the 4907199 address and keep zero-source 4908179 unassigned. Nearest competition retained 4907199 assigned coverage 26,693/37,999=70.25%, while 12,771/22,029 eligible 4908179 pixels were assigned to 108247349 (conditional wrong rate 1.0). Blanket inactive veto removed all 37,999 target pixels of 4907199. Existing L_depth seeds were zero in both target components; projected initial-3D-point propagation avoided wrong assignment but addressed only 2,402/37,999=6.32% of 4907199. Using raycast building id or LoD2 roof z would exceed the locked audit-only/class-label scope. The first smoke also exposed float32 UTM-offset quantization in the producer; after promoting coordinates before offset restoration, all six projection inventories matched the committed T0-2 counts and the smoke was regenerated. Full 428-view cache generation is hard-blocked; the 1k gate and Track A training were not started pending an address-rule decision.
- 2026-07-13 S3-A address-rule resolution: Kim Hwiyoung selected option B and re-scoped S3-A to a class+instance oracle mechanism upper bound. The fixed clean class mask's actual-source raycast (`geoid=48.0 m`, `shift_z=556.0 m`) may provide building ID only as the loss address R; it must not provide depth, height, or plane-value supervision. Official datum 45.7 remains audit-only and DATUM-001 remains open. S3-B is reserved to run without oracle building-ID addresses. The previous footprint-rule smoke remains a historical defect baseline, not an active blocker.
- 2026-07-13 S3-A pre-run contract review: before any 428-view production or learning, review found that visible views with no retained region were omitted from the P-J/P-L median, a half-weight regate could replace attempt-1 classifications, and the raw set-count parser lacked the intersection lower bound. The implementation now records such visible views as zero, validates set partitions, freezes attempt-1 medians/classifications/boundary tags against the committed gate CSV and raw-source SHA-256, and keeps attempt-2 sweeps diagnostic-only. A transient guard-edit regression that would have disabled full-run semantic loss was caught before execution; loss initialization/effective config remain active for full runs and only the P-J/P-L sweep is gate-only. No S3 learning had started; the complete Docker S3 test bundle passed 45/45 afterward.
- 2026-07-13 S3-A first canonical T0-1 pass: all 428 oracle-address cache files were generated, but the producer stopped before the T0-1 CSV/gallery/manifest because it had incorrectly reused the 64 px fragment-measurement support threshold as the visible-view threshold; 8568392 therefore exposed only 2 views under that accidental rule. Visibility is now `official P_ref > 0`, while 64 px remains only the fragment-support threshold. Selected `<64 px` views are retained as explicit `LOW SUPPORT` audit rows/overlays, and an unavailable boundary offset remains null with an undefined reason rather than being imputed. The corrected canonical rerun passed 428/428 cache files, 570 regions, 27 overlays, and strict loader preflight; the corrected Docker S3 test bundle passed 46/46. No T0-4 gate or learning started before this recovery was committed.
- 2026-07-13 S3-A gate-config provenance preflight: the first `generate-configs` invocation wrote three YAMLs, a four-row inventory, and a config manifest, then stopped before `versions.txt` because the orchestrator compared the valid host-inspected image digest with in-container `not_available:docker`. No training command or result file existed. The provenance helper now accepts a valid supplied sha256 only for the exact unavailable-CLI case, still rejects malformed values and real-inspect mismatches, and does not bypass executable inspect errors. The three configs, inventory, manifest, and versions were regenerated under the corrected committed helper before any learning.
- 2026-07-14 S3-A attempt-1 gate audit invocation: the completed gate run itself returned 0, but the first read-only `gate-audit` invocation used the standard `/workspace/JointBuildGS` outer mount and failed command-string provenance because the immutable launch recorded the real `/media/.../JointBuildGS` host source. No raw run or CSV was changed by that failed invocation. Re-running the same audit code with the original absolute-path mount validated the launch hashes and produced the canonical CSV. Attempt 1 then mechanically failed (`L_semdepth` max grad share 0.64322; 8568391 P-I zero), so only the preregistered half-weight re-gate remains authorized before Track A.
- 2026-07-14 S3-A half-once gate branch: halving both semdepth terms to 0.125 reduced the combined maximum gradient share to 0.30118; boundary-normal remained below threshold at 0.00328 and total loss stayed finite. P-I remained zero for 8568391 (0 positive region rows out of 8) while 4907199 and 8568392 were positive. The final mechanical gate status is therefore fail. Under the locked branch, `L_semdepth` is not eligible for Track A; no further re-gate, partial `L_nb` full cell, r1/r2 training, readout, or scoring was started. Attempt-1 P-J/P-L labels remain frozen: textureless three all P-L at median 0 valid depth-anchor pixels; collapse three remain preregistered P-J.
- 2026-07-14 S3-A human gate reclassification honesty record:
  - 이 재판정은 게이트 결과를 본 뒤에 내려졌다.
  - 근거 = ① 신호 도달을 당락으로 잠근 문면이 없음.
  - ② "3채 전부 신호 통과" 요구는 씨앗 부재 갈래의 사전 등록과 논리 충돌(무반응이 예측인 건물의 무반응이 게이트를 죽이는 구조) ③ 비중 기준은 통과.
- 2026-07-14 S3-A human admission ledger: Kim Hwiyoung classifies signal delivery (P-I) as a prediction measurement rather than a gate criterion. The attempt-2 mechanical fields remain unchanged (`gate_status=fail`, P-I reason retained), while the separate fields are `human_verdict=pass_by_kim_20260714` and `human_verdict_reason=P-I reclassified as measurement`. Track A is authorized only at the carried half weights `0.125 smooth + 0.125 plane + 0.01 boundary-normal`; no further re-gate or weight discretion is allowed.
- 2026-07-14 S3-A human-admission record recovery: the first adapter invocation atomically appended the two authorized human fields, then stopped before config synchronization because the historical half-once recomputation guard requires the gate CSV to remain identical to committed HEAD. No mechanical cell, config, run directory, or learning output changed. The adapter now resumes only after comparing every original field and row against `HEAD` and proving that the dirty CSV differs solely by the exact two human columns; the resumed invocation then generated the half-weight full configs without starting learning.
- 2026-07-14 S3-A Track A launch: after commit `cad4c39` was pushed, both immutable dry-runs reported committed inputs, zero artifact collisions, oracle cache 428/428, and exact weights `0.125 smooth + 0.125 plane + 0.01 boundary-normal`. r1/r2 then passed independent 428-view loader preflights and entered 30k training on GPU 0/1 respectively. Both logs confirmed 245,641 initial Gaussians, no monocular-depth maps or weight, and the locked Arm 1-prime base; no additional learning cell was started.
- 2026-07-14 S3-A geometric-FM seed preflight STOP: the complete local MASt3R metric checkpoint was verified (snapshot `06e7259f34c3060f322df5cb0c7b9094f57e41fc`, weight SHA-256 `0a615eb05fa9db654050aa655945ee5696e7c6c1b7f93f1ee8c37249010f6feb`), but MASt3R/DUSt3R/CroCo code, a pinned Docker runtime, and the DUSt3R fallback weight are absent. GPU 0/1 are occupied by the two S3-A runs, and a reproducible pose-constrained metric setup within one hour is not established. Per the stop rule, no install, download, inference, seed generation, or training input occurred; the check is recorded only as seeding-review material.
- 2026-07-14 S3-A Wave 2 locked observation: r1 `step_005000.pt` SHA-256 is `23282ebc56b4cfaa479b144417e8c4612b3748b588d7cb59a15ae8e1ee7cb3e4`. Exact-footprint Gaussian counts, without semantic-class or roof-height filtering, are collapse `4907202=432`, `4908168=1`, `4908178=112`; textureless `4907199=129`, `8568391=14`, `8568392=1`; reference `4907184=13951`. These are early inventory measurements, not formation-height, genuine-assembly, or survival verdicts; survival remains paired with region-specific semantic-guided gradient inflow at later checkpoints.
- 2026-07-14 S3-A mirror reconciliation: local HEAD `6056a23` initially led origin `59b025b` by 25 commits, explaining why the morning mirror lacked the gate/report sections. The existing commits were pushed without rebasing or changing branches; before the ID/class supplement task, local and origin were both `ca9a451`.
- 2026-07-14 S3-A ID/class supplement recovery: the first atomic replay stopped at `DJI_20241217094552_0032_D` because a cross-process bit-exact guard saw inventory/replay class mismatches `74/80`; a retry also left no output when its PTY result was lost. The strict source-manifest, producer, datum, mesh, fixed-PNG, 428-cache, and container locks remain fail-closed, while replay pixel delta is now a recorded non-gating measurement (final: 358/428 views bit-exact; pooled inventory/replay mismatches 123,232/161,311). One later invocation used the wrong image-ID environment variable and failed before raycasting; the corrected invocation then atomically stopped because frozen `8568392` rank 2 had no actual-source target-ID pixel. The final renderer preserves all nine frozen crops, draws eight real ID boundaries, explicitly annotates the one non-visible ID, fabricates zero boundaries, and writes the CSV/gallery/durable manifest only after full-bundle QA.
- 2026-07-14 S3-A checkpoint-gradient fixed-view recovery: the first CPU-only preflight stopped before loading any checkpoint or rendering on GPU because the prior reference-area top-3 contained zero committed oracle-address pixels for `4908168/DJI_20241217084851_0189_D` and `4907199/DJI_20241217102653_0002_D`. Before consulting any checkpoint result, the fixed rule was replaced by all TRAIN views with positive committed-cache target-address support, sorted by address pixels descending and stem ascending. The resulting top-3 counts are recorded in the manifest, and the 228-row read-only sweep retained the claim boundary “posthoc checkpoint gradient potential; not exact online optimizer-time gradient; no FM/paper claim.”
- 2026-07-14 S3-A full-run gradient-share observation: the half-weight cells completed with finite loss and return code 0, but the 500-step single-random-view audit sampled combined semantic-depth gradient shares above the original 0.40 gate scale after admission: r1 maximum `0.847824` at 7.5k (plane-only maximum `0.458203` at 20k), r2 maximum `0.894032` at 4k. Boundary-normal maxima remained `0.030624/0.027510`. These sparse per-view samples are safety observations, not a re-run of the locked 1k gate and not a new verdict; no weight or training cell was added afterward.
- 2026-07-14 S3-A 8way panel environment recovery: the first panel invocation used `jointbuildgs:dev`, whose vendored `laspy` had no LAZ decompression backend, and stopped on the first building before writing any panel or inventory row. `jointbuildgs-p0-tools:t0` was then verified to expose `laspy 2.6.1` with `LazrsParallel/Lazrs`; the unchanged panel command in that fixed image produced six panels and the exact 60-row (6 buildings × 10 sources) inventory. No data, score, or panel-source selection changed.
- 2026-07-14 S3-A read-only QA command recovery: one post-pairing private-contract check first treated the hyphenated phase directory as an importable Python package, and one gradient-summary check first requested the nonexistent `iteration` column instead of `step`; both failed before writing files. File-path import and the locked `step` schema retries passed (228-row pairing contract and full-run summary respectively).
- 2026-07-14 S3-A-prime learning-zero measurement commit ledger: boundary inventory = repo commit `3d6d2d0015e4eb423dae3b62fa9fd757ae8df4d7`; pinned MASt3R environment and smoke = repo commit `344ebccd90c764e07070b1998188daee0a48cb7d`; MASt3R three-building rescore = repo commit `96457e548f7142c1f4f355194f08445004189a0e`. All three artifacts record `learning_runs_started=0`; none was used as a seed or training input, and no interpretation/verdict was written into their manifests.
- 2026-07-14 S3-A-prime boundary-inventory recovery: the first dense-point multiview reprojection indexed native `669x421` PatchMatch depth maps with `914x574` camera pixels and therefore produced false zero agreement. The mismatch was caught before commit; projected pixels were scaled to each native depth-map shape, all CSV/overlays/manifests were regenerated, and the committed output hashes were revalidated.
- 2026-07-14 S3-A-prime MASt3R recovery and retained exception: the first smoke inference completed but its provenance write stopped on Git dubious-ownership inside `/opt/mast3r`; no smoke artifact was written, explicit per-repository `safe.directory` reads were added, and the exact smoke rerun passed. During full scoring, two discarded pre-commit alignment attempts exposed that centered MASt3R crop-gauge pointmaps cannot be treated as the off-center physical crop camera frame. The committed method recovers both physical cameras in the crop gauge and aligns them to fixed COLMAP poses with one camera-only similarity before any footprint/LoD2 scoring. Final scoring completed 15/16 selected pairs; `8568392` rank 2 remains an explicit `failed_crop_gauge_alignment` row (`joint_inliers<4`) rather than being removed or replaced.
- 2026-07-14 S3-A-prime fixed-pose retriangulation recovery: the first driver invocation used the obsolete FM manifest key `docker` instead of `runtime_lock` and stopped before processing any pair. The lookup was corrected and the full unattended rerun recorded all 16/16 pairs, six overlays, three building summaries, and `learning_runs_started=0`.
- 2026-07-14 fair-pilot prepare recovery: the first Area 3 preparation used incompatible `ogr2ogr -a_srs` and `-t_srs` arguments in one call and stopped before MVS. Source-CRS staging and output reprojection were separated; the ten-view fixed-pose workspace and EPSG:25832 reference assets then completed.
- 2026-07-14 fair-pilot MVS recovery and retained partial: the first hand-built dense workspace omitted PatchMatch output directories and later `fusion.cfg`, producing missing-depth errors and fusion return code 134. The wrapper now creates the required directories, carries the fusion config, pins image IDs and cache inputs, and enforces the whole-stage time bound. The resumed ten-view depth run completed, while geometric and photometric fusion both retained 0 points; official Match-T DIM, ALS staging, 57-building metrics, and seven locked candidates remain separately recorded, with overall status `partial` and GS learning 0.
- 2026-07-14 overnight A/B commit ledger: fixed-pose MASt3R reciprocal-match retriangulation = repo commit `0ccf80f37ae97748b8ade0b80d91088ab1dde80d`; independent Vaihingen Area 3 fair-pilot preparation = repo commit `1e06f6040e5031c556413ffd2bbfa5150b1cb304`. Both commits record training/learning runs as 0; the latter keeps the new fixed-pose COLMAP result explicitly `partial`.
- 2026-07-15 S3-A-prime FM retriangulation rescore Wave 1 ledger: actual LoD2 RoofSurface/GroundSurface fixed-COLMAP registration QA = repo commit `9d3f409cf67d912bd281c909c5ed53e0eb3a1c8b`; registration CSV SHA-256 `50fa465a18f991905cd94c01d9ddb01e7d75c2c83bbd51ecabc66ba109b3e72f`; registration manifest SHA-256 `5616911bf8cbf612383f3e8e51b4736c6f96ceb00cd1a3c85850fada37d8cfce`. All three targets are recorded as `seated`; 8568391 views 0194/0195 are frame-clipped and do not vote, while 0023 is the full-perimeter anchor. Learning and new MASt3R inference runs are both 0; these are registration classifications, not a seed-source verdict.
- 2026-07-15 S3-A-prime FM retriangulation rescore Wave 2 ledger: cached-DLT all-inside height, robust-plane, and footprint-intersecting-cell coverage rescore = repo commit `13032e44e61a47d392889dff1a12a636327fbfb2`; rescore CSV SHA-256 `f720a5687da54dac276c27222e49f8012f2b24be3e2bbcaa5cf7e15a21faf299`; final manifest SHA-256 `14487ac30c0e8524d6e00be3890ef6802366640c864dd8d1e7fba43cc567216e`. Numeric classes are `4907199=ii_near_height_scattered_or_low_coverage`, `8568391=i_seed_usable`, and `8568392=ii_near_height_scattered_or_low_coverage`; 4907199 retains the sole nonzero eligible pair `0023×0048=373`, with 0048 recorded as a Wave-1 frame-clipped non-voting view. Ground elevations use observed SfM+dense-init points while the supplied footprint geometry provides the spatial masks; the cached DLT lineage retains the source run's frozen T0-1 oracle-address crops. Learning and new MASt3R inference runs are both 0; these are numeric classifications and provenance observations, not a seed-source verdict.
- 2026-07-15 S3-A-prime Phase 0 ledger: FM dense dial, mono-normal diagnostic, P0 plane-fill/Roofer read-out, and MVS hole measurement = repo commit `91c321cb11d90cff20b1e35833a83e0f2daf628d`; FM CSV SHA-256 `5a743961cd58dc099dce3200a2465d0838dc9b4dce4f59fb787769064d4a9a26`, mono CSV `51725eec0953b59ffabf30bf8e4a468909b4352d3749fe8be9976e07db0e054e`, P0 CSV `4942ffcfa14ca5f348eb924652d20ba4528c3f18fab5bf1fd9d667425e78f536`, MVS CSV `88cb9003b35906e167006b5f9ca9f5403814971343f3ca6da3c54cf0f017aea0`; FM/mono/P0 manifests SHA-256 are `2d5018269e7305e6bd1b720dc62f7ff21ffa052c0666a3c40c521ef2cc5f2a8e`, `62c4d9c3445e384bbd8d5c99ea16946ac7b369e5aa668be71542d99344885928`, and `3640ece36bb321ddf96b07881cd07db83dbaf61515bc3d6ce3e73bcab349cc83`. The locked B-a operating point is absent for both 4907199 and 8568392; all three P0 read-outs contain raw LoD2.2 geometry but are retained as `lod11_fallback` with accepted `has_lod22=false` and `substantive_filter=false`. Learning runs are 0; new MASt3R inference is confined to the 13-pair dense dial, and LoD2 remains score/overlay only. These are measurements and rule-based classifications, not a seed-source or experiment verdict.
- 2026-07-15 S3-A-prime Phase 1 ledger: canonical P0 seed surfaces and B-c boundary-point classification = repo commit `fb6cc52773a020c64a74f717174f88f269ceb092`; seed manifest CSV SHA-256 `0f6c27e35da93855d40ca3f0dcaea731368438bfda0e0fac35a787bb48b0d698`, B-c CSV `03e7e69992357b511f37ad0ff15f5b36e749a6a18dd31aef19fa00ee5d586937`, report `dca443df42c1a77395c94e1ecd39a4b39401633775259294f92b0541c0f6578c`, and run manifest `ba5c751c8e277f0d22c780afb85e31cd5316ea81aefa272e6ccbc12df1cf39ff`. Canonical surface-seed NPZ SHA-256 values are `4907199=146a5134cb8958a98296ce5252852326f58a796bb1f6d58707d4655b0bec39ce`, `8568391=237fbb2f4a4737cef95874f73bc46b365bc55c574a65c52dfebabd92b1fbf33e`, and `8568392=2c4a6a4b48b14658ac84c3d3f827099e7043e81f7bc443c27d9c436f95178c5e`; the two-point 8568392 B-c auxiliary NPZ is `36db6a0df35236638083cea52989f1b66edd91476f8695dc004e16beb2aec86c`. All three canonical seed payloads are byte-identical to the Phase-0 float32 surfaces; B-c eligible counts are `4907199=0` and `8568392=2`. Learning and new MASt3R inference runs are 0, and LoD2/ALS are excluded from seed generation and boundary classification. These are measurements and rule-based classifications, not an experiment verdict.
- 2026-07-15 S3-A-prime Phase 2 engine ledger: explicit fixed-view roles, strict 0.10-opacity surface-seed lineage, target-region mono-normal and affine-SSI losses, and the A2 surface-only prune schedule/protection were added in repo commit `55a43731b1dc39b3cfc336a84da69562d36307db`. Docker compile plus the focused and existing semantic/region/P-J/P-L regression bundle passed 42/42; the independent review also verified zero gradient outside the addressed region and gsplat row-wise lineage propagation. No training run was started by this engine task, and no LoD2/ALS geometry enters the added training paths.
- 2026-07-15 S3-A-prime Phase 2 prepare ledger: locked input staging, offline boundary-frontier graph propagation, exact 42-job inventory, two-GPU unattended runner, runtime/input/checkpoint fail-closed validation, and job/config checkpoint binding were added in repo commit `a1c48cfcfc85507a7a98ccd3b527491e729e9e2d`; the regenerated config/inventory bundle is repo commit `04e59f1`. Lock, inventory, and prepare-manifest SHA-256 values are `b131c85c66442a9f91d4001684a897cbb9fb707a57879869279f16d39cd137ee`, `57cd72e27aba96e412a3e57de1003551f67b40b6d321d0c048261b58effe34df`, and `3974a3a39955a42125115d31ffbeb6584804b41b0ae18a8972acac1c38ee768c`. The inventory is `18 base + 24 nonzero-height perturbation` jobs and the dry-run records all 42 as pending. For 8568392, A1/A2 input is P0 37 + classified B-c auxiliary 2 + footprint-interior graph propagation 36 = 75 surface seeds; propagation z comes only from the FM-anchored P0 plane. At this ledger point training runs started are 0 and GT/LoD2/ALS flags are false for all 42 jobs; these are preparation counts and provenance records, not an experiment verdict.
- 2026-07-15 S3-A-prime Phase 2 CUDA smoke v1 STOP: the three smoke-only jobs loaded the 8568391 fixed-view SfM subset and strict 0.10-opacity surface seeds, then all stopped before the first rendered training update because gsplat JIT attempted to create the unwritable `/.cache` under the locked `--user` mapping. No final checkpoint was produced and the 42-job Phase-2 inventory remained entirely unstarted. Failure bundle repo commit is `36b25c8`; manifest SHA-256 is `9bd86770419587823454b860c02d27895762ed50c90b0db2d104af06540a2ea2`. Recovery is limited to pinning writable HOME/XDG/Torch-extension cache paths in both launchers and rerunning the same smoke contract in a new output path; no experiment threshold or input changes.
- 2026-07-15 S3-A-prime Phase 2 CUDA smoke v2 retained partial: writable HOME/XDG/Torch-extension paths were pinned and A1/A2 completed, but the first cold-cache parallel A0/A1 launch exposed a same-extension JIT build-directory race; A0 stopped while A1 built the shared extension. The retained measurements are A1 final `it=2`, `n_prim=1462`, jointly eligible mono-target audit rows `1`; A2 final `it=702`, `n_prim=1232`, jointly eligible rows `71`, first refine/protection at step 600 with protected count 339, candidates 465, protected candidates 44, pruned 421, effective prune opacity `0.0500000007`. The 42-job inventory was still unstarted. Partial bundle repo commit is `6a11941`; manifest SHA-256 is `83b8dab16b410b39e14f58e745578ba269d71f0b6910863ec65799bc96b9a649`. Recovery is limited to a serialized single-process gsplat prewarm before parallel workers and a new smoke output path; no arm, input, loss, or threshold changes.
- 2026-07-15 S3-A-prime Phase 2 CUDA smoke v3 ledger: writable runtime caches and serialized gsplat prewarm are repo commits `95afb45` and `4e6dabb`; the final regenerated prepare artifact is `9ddc3e8`. Final Phase-2 lock, 42-job inventory, and prepare-manifest SHA-256 values are `9f62fe3d5756b02bbd3f4b495b183e68980097afaf3dae55369e97a902075dce`, `57cd72e27aba96e412a3e57de1003551f67b40b6d321d0c048261b58effe34df`, and `3f99dd5983872d9ef882ecdb9382837887aec2ae6b8a0165889a9c72999ddf75`. The serialized prewarm loaded extension SHA-256 `b291971546d350951760d34863ff96068c8ef018dcdeaaf0d61ec21471baadd5`. All three new-path CUDA smokes completed: A0 `it=2/n_prim=1462`; A1 `it=2/n_prim=1462` with one jointly eligible mono-target audit row; A2 `it=702/n_prim=1228` with 71 jointly eligible rows and first refine/protection at step 600 (`protected=339`, `candidates=467`, `protected candidates=44`, `pruned=423`, effective prune opacity `0.0500000007`). Bundle repo commit is `c8a831a`; manifest SHA-256 is `946374e2d16e3ae10d011d3b838203ca091e22b3458d54f49a1fa21bbdbadf73`. These three short optimizer smokes are explicitly excluded from the 42 locked 30k experiment jobs; all 42 remained pending at this ledger point, and GT/LoD2/ALS input flags are false.
