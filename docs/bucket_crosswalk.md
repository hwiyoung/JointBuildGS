# bucket_crosswalk — 옛 d12 버킷 × 새 분해 대조 (읽기·판정 금지)

> **박사연구 GS-JSO · 모집단 잠금 보조 v4 [2].** 브랜치 `feat/p2-structure-learn`. EPSG:25832. Docker(`--user`).
> **읽기 + 소규모 계산**(분해 규칙 적용), 재구성/재학습 없음. 관찰만, **라벨·τ 확정=김휘영**. 커밋 `population-lock-aux-v4`.
> 재현 `phases/p2-gsjso/scripts/classify_v4.py`(tools:t0). CSV `docs/bucket_crosswalk.csv`(48행, flag 멀티-핫).

## 규칙 (합의; τ만 [1]로 갱신) — 상세 정의는 CSV 헤더·[[texture_anchor_check]]

생성축 48동(ALS✓∧DIM✗∧reason≠missing_lod22) 대상. 우선순위 A-first(규칙 서술 순서), 단 **모든 플래그를 멀티-핫으로도 기록**(김휘영 재정렬용).
- `a_cand = frac_views_incidence_le60 < 0.315 AND recon_score_median < 103.6`(대조군 p10)
- **거부권**(사실): {sparse,acmp,gs_sparse,gs_dense,gs_acmp} 중 하나라도 has_lod22=1 → **경계_방법회복**(A에서 제외)
- `A1_촬영확실`(a_cand ∧ recon<31.8) / `A2_촬영경계`(나머지 a_cand)
- `B_무텍스처`(**roof_lowtex_v4 ≥ 0.761** = 대조군-114 p90) / `E_폐색`(occ≥0.667) / `F_소형`(면적<29.8) / `미분류`(C 정반사 병합)

## 새 분해 분포 (48동)

**A1_촬영확실 11 · A2_촬영경계 4 · 경계_방법회복 6 · B_무텍스처 2 · E_폐색 5 · F_소형 2 · 미분류 18.**

## 옛 버킷 → 새 분해 크로스탭

| 옛 버킷(n) | A(A1+A2) | 경계·방법회복 | B_무텍스처 | E_폐색 | F_소형 | 미분류 |
|---|---:|---:|---:|---:|---:|---:|
| ③_coverage (36) | **14** | 5 | 2 | 2 | 1 | 12 |
| ④_impossible (7) | 0 | 1 | 0 | 2 | 0 | 4 |
| ①_textureless (5) | 1 | 0 | **0** | 1 | 1 | 2 |
| (②_assembly 16 = missing_lod22 = 새 분해 범위 밖) | — | — | — | — | — | — |

## 이동 요약 (관찰, 판정 금지)

1. **옛 ③커버리지 36의 절반이 새 A(촬영 탓)로**: A 14(A1 11+A2 3) + 경계·방법회복 5 → **19/36이 촬영-기하/방법 축**. 나머지 B 2·E 2·F 1·미분류 12.
2. **옛 ④불가 7 → 새 A는 0동**: 촬영조건이 A候補(inc60<0.315 ∧ recon<103.6) 임계엔 미달 — E폐색 2·방법회복 1·미분류 4. "불가"가 촬영-기하만으론 설명 안 됨.
3. **⚠ 옛 ①무텍스처 5 → 새 B_무텍스처 0동**: 5동 모두 roof_lowtex_v4 **0.348–0.748 < τ 0.761**(4908160 0.695·8568392 0.737·8568391 0.748·108247351 0.499·108247350 0.348) → B 미분류·A2 1·E 1·F 1·미분류 2. **T9가 무텍스처로 확정한 동조차 대조-p90 τ를 못 넘음** → **τ(0.761)가 높다는 [1] §3 관찰을 강력 재확인**(τ 하향 시 이들이 B로 복귀). 판정=김휘영.
4. **경계·방법회복 6동** = a_cand였으나 다른 방법(GS/ACMP/sparse)이 has_lod22 회복 → "촬영 탓 A"에서 사실상 제외(방법이 회복시킴).
5. **미분류 18**(예상 ~13보다 많음) — 주로 §3 탓 무텍스처-경계(roof_lowtex_v4 0.5–0.75)가 τ 밑에 깔림. 정반사 플래그 2동(4907014·4907015). 증거카드 `evidence_cards/`로 수동 검토.

> 재현: `docker run … jointbuildgs-p0-tools:t0 python3 phases/p2-gsjso/scripts/classify_v4.py`. flag 멀티-핫(a_cand·flag_B/E/F/C·veto_recovered) = 김휘영이 우선순위·τ 재정렬 가능.
