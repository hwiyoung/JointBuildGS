# P2 현재 프레임 핸드오프 (2026-06-29) — feat/p2-fidelity

> 상세 정본 = (Cowork) **P2_정본_v1_20260629.md**. 이 파일은 repo 작업용 요약+포인터다.
> 방향·판정 = 김휘영(이미 정함). 에이전트는 수치·관찰까지.

## 핵심 주장
- **모델 없이 영상에서 CityGML 생성** (GS4Buildings의 refinement과 구별 · **최소 비모델 prior**).
- 출력 = **구조화 CityGML**(메시 아님).

## 프레임 4갈래
- **생성**: ① 무텍스처 · ② 조립(**방법 표적**)
- **품질**: ③ 과분할 · ④ 정확도
- **닫힌 모델(valid-solid)** = ③④ 결합지표 + 표준 수리.

## 정정된 이해 (D6 점검 결과)
- 지붕유형 **눈대중 라벨 다수 오류**(4906969 곡면→**단차 평지붕** 등) → **곡면 과분할 레버 전제 무효**.
- 생성 7/8의 속: 무텍스처 4 = **낮은 슬랩**, 조립 3 = **충실도 미검증**.
- **순환 아님**(씨앗 = MVS). **L_sem 현재 = LoD2 오라클(검증용)** · 실제 = **영상 의미분할 예정**(미실행, 현 결과 = 상한).

## 평가 틀
- 같은 데이터 **ALS 상한에 상대적**(외부 절대바 아님); 문헌(Rottensteiner 2014 ALS 0.2 m · SAT2LoD2 영상 0.5 m)은 맥락.
- **"≤2 m = 유효" 폐기** (유효성 = 위상 val3dity).
- 정본 평가 틀 = **mob**(survey w3_2b와 **비호환 · 하네스 내 비교만**). 면수 = **target-only**. **`d5_target_facets.csv`는 stale → 현 디스크 재계산.**

## 실험 순서
1. **조립 충실도** → 2. 과분할 레버 → 3. 무텍스처 높이 앵커 → 4. 정확도(P3) → 5. 영상 의미분할 검증(데이터 교체 · 맨 끝).
- 의미 라벨은 **오라클(GT)로 고정**해 알고리즘부터 시험하고, 영상 의미분할은 **끝에 데이터 교체**로(알고리즘 결함·라벨 노이즈 안 섞기).

## 첫 실행
- **`docs/W_assembly_fidelity.md`** (조립 충실도; 대상 **42364659 · 42364663 · 4907510**; ALS 상대 판정).
- 사전등록 = 정본 §5.

## D6 점검 산출 (재정립 증거 · `feat/p2-d6-curved` = tag `p2-d6-audit-done`)
- `docs/W_D6_overseg_diag`(step0; 곡면 과분할=입력측 GS·Roofer 임계 비지시) ·
  `W_D6_survey`(참조 roofType≠관찰; 통제 93동 참조-곡면 0동) ·
  `W_D6_shape_audit`(곡면 0동; 4906969=단차 평지붕·4906972=박공; 생성 raw 0/8→GS 7/8→ALS 7/8) ·
  `W_D6_prior_provenance`(LoD2 z 직접순환 없음; sem_detach_geometry=false로 L_sem이 LoD2 클래스투영으로 GS 기하 부분견인) ·
  `W_D6_textureless_fidelity`(무텍스처 4동="조립됨≠충실": 높이 0/4 ~2.5 m 낮음·외쪽 4907182 평슬랩 뭉갬).
- 모두 `docs/evidence/p0_g1_20260613/`에 패키지로 보존.
