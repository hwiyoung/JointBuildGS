# Experiment dashboard

Static dashboard templates and selected checked-in UI state. Rebuild through
`scripts/inspection/experiment_dashboard/build_dashboard.py` inside the project
container. Inputs are resolved from `JBGS_ARTIFACT_ROOT`, never from a repository
`results/` directory.

