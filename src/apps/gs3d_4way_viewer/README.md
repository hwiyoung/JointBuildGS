# GS 3D viewer

Static inspection UI for splats, surfels, turntables, and raster comparisons.
Run it through `scripts/inspection/serve_gs3d_4way_viewer.py`; the server exposes
`JBGS_ARTIFACT_ROOT` as the read-only `/artifacts/` URL namespace.

