# Automated checks

Repository-wide tests live directly under `tests/`. Experiment-driver tests live under [`experiments/`](experiments/README.md) and mirror the family names in `scripts/experiments/`.

Run checks in the repository containers; do not install host dependencies to satisfy a test.
