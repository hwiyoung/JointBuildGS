# P2-D4 사양서 · 사전등록 (pre-registration) — 정규화 재학습으로 GS 굴곡 펴기

> **2026-06-25 · branch `feature/p2-prior-full` · 판정=사람(김휘영) · 골대이동 금지.**
> 이 문서는 **본런 전에 잠그는 사전등록**이다. §6 판정기준·§7 cp 사다리는 결과를 보기 전에 확정한다.
> 작성=에이전트(관찰·산출까지). §6/§7 **확정·판정=김휘영**. 상세 사전점검 근거: [[W_D4_precheck]](docs/experiments/w_d4/reports/W_D4_precheck.md).

## §0 한 줄
D의 **손실 균형**(cp 단독 지배)을 **항별 ÷측정평균 고정 정규화**로 바로잡아, 노이즈 depth/normal 감독을 걷어내고
**평면화(cp)를 발화**시켜 GS 지붕 굴곡을 편다 — **품질을 GS 방법으로**(Roofer 튜닝 아님). 동시에 생성(D 7/8) 유지 확인.

## §1 가설
- **H1 (메커니즘)**: D에서 cp 원시손실이 평탄(미발화)했던 것은 cp 가중이 낮아서가 아니라, **노이즈 외부 depth/normal 감독이
  기하를 wavy MVS 면에 핀**해 cp가 작용 못 했기 때문. normal→0·depth↓ 로 핀을 풀고 cp를 photo·sem과 **동등 share**로 두면
  cp가 비로소 면을 편다.
- **H2 (품질)**: 굴곡이 펴지면 같은 고정 Roofer(epsilon 0.3 등 불변)에서 **과분할↓** + 평지붕 **RMS→LiDAR 수렴**.
- **H3 (무열화)**: depth를 낮춰도 생성(조립 7/8·회복동 밀도)은 유지되고 제어동은 D보다 안 나빠진다.
- **반증(곡면 위험)**: cp는 큰 곡면을 한 평면에 과병합(4906969 gid1828 15,008점 RMS 4.13 m, [[W_D4_precheck]] §1라) →
  cp 발화가 **진짜 곡면 구조까지 뭉개는 과-평탄화**일 수 있다. E 정성으로 분리 관찰.

## §2 변경 = config-only 손실 균형 (엔진 무변경) — **CORRECTED 2026-06-25 (김휘영)**
> **정정 기록**: 초기 D4 는 **정규화 과적용**(photo 5.6·nc 2.1·sem 0.92 로 전 항을 share~1 로 끌어올림) → 사전점검서
> 초반 photo/psnr 열세 발생(nc 2.1 과제약, [[W_D4_precheck]] / 본문 §3 경험표). **정정** = "**cp만 정규화**" 최소 config:
> D의 **건강한 항(photo 1.0·nc 0.05·sem 0.1·na 0.08)은 그대로**, **문제 3항만** 수정(cp 0.08→0.01·depth 0.1→0.03·normal→0).
> 골대이동 변형 아님 = **config 오류 정정** → corrected D4 로 재-lock.

유효 스칼라(= train.py `loss_total` 에서 raw 를 곱하는 가중) 와 config 매핑:

| 항 | 유효 스칼라 | config (D → D4) | 근거 |
|---|---|---|---|
| photo | **1.0** | `w_photo` 1.0 (유지) | D 건강항 |
| depth | **0.03** | `w_depth` 0.1→0.03 | de-noise(CV 1.74 노이즈 MVS), valid 마스크 유지 |
| normal(외부) | **0** | `w_normal` 0.15→0.0 | 외부 MVS 법선 제거(노이즈·깊이의 노이즈 파트너) |
| nc | **0.05** | `w_nc` 0.05 (유지) | D 건강항(내부 2DGS 일관성; 2.1=과제약→열세였음) |
| sem | **0.1** | `w_sem` 0.1 (유지) | D 건강항 |
| structure cp | **0.01** | `w_structure`·`w_structure_cp` → 1.0·0.01 | de-dominate: cp 68%→~31%(photo parity) |
| structure na | **0.08** | `w_structure`·`w_structure_na` → 1.0·0.08 | = D 유효 na(유지) |

- **매핑 방식**: `w_structure=1.0` 으로 두어 **sub-weight 가 곧 유효 스칼라**가 되게 함(cp=0.01, na=0.08). l_structure total = na+cp 합([structure.py:54](src/stage2/loss/structure.py#L54)).
- **MUST-EQ(비손실) 그대로**: `sem_detach_geometry=false` · `max_iter 30000` · `data_root=data_geoidfix` · `seed_protect` ·
  densification(D dense) · 스케줄(depth ramp@5000 / **structure 게이트@15000** / photo·sem·nc 상수) · G2 현행(voxel 2.0) ·
  read-out=gssem · Roofer 고정(epsilon 0.3·min-points 15·complexity 0.888, 건물별 튜닝 금지).
- **delta 기록**: `runs`=`results/tum_transfer/mob/gs_d4_{dense,acmp}/versions.txt` (run_d4.sh 가 스탬프).

## §3 사전점검 (본런 전 — 표 먼저)
### ① 가중 기여 균형 — **분석적(완료)**. 어느 항도 total 의 ~40% 안 넘나(D 지배 사라졌나).
유효 스칼라 × D-run raw-mean → 가중 기여 share:

| 항 | D-0.3(감사) | D-0.08(보고된 D) | **D4 (corrected)** |
|---|---:|---:|---:|
| photo | 2.9% | 8.4% | **30.7%** |
| depth | 5.2% | 15.1% | 16.6% |
| normal | 0.8% | 2.3% | 0% |
| nc | 0.2% | 0.6% | 2.1% |
| sem | 1.8% | 5.2% | 19.0% |
| **cp** | **89.1%** | **68.5%** | **31.4%** |
| na | ~0 | ~0 | ~0 |
| **MAX** | cp 89% | cp 68% | **cp 31% (<40 ✓)** |

→ cp 지배 **89%→68%→31%** 붕괴(cp 0.08→0.01). D4 는 **어느 항도 40% 미만**, **cp 31% ≈ photo 31%(평면화=photo 동등)**, nc 2%(=D, 과제약 없음). 사전점검 ① **충족**.

### ②③④ — **경험적(소수 step, GPU)**. ⚠️ **gate 주의**: cp 는 `structure_warmup=15000` 이후 활성 → 짧은 런으로 ②(cp 발화)를 직접 못 봄.
- **② cp 발화** = cp 원시가 줄기 시작하나(D 평탄). **본런 §3 모니터로 확인**(15k 이후) + 필요 시 §7 사다리. 사전 진단으로
  `structure_warmup` 을 임시 하향한 별도 short-diag 로 조기 관찰 가능(단 미정착 기하 위 그룹 = 근사).
- **③ 안정** = NaN/발산 없음 + **photo 무열화**(w_photo 5.6 의 전역 스케일↑ 위험; Adam 은 전역 상수에 거의 불변이나 확인). short-run(~2–4k step)으로 관찰.
- **④ 생성 조짐** = 저밀도 회복동(4907182 등) footprint 점이 D 대비 안 줄었나. densification 은 500부터 → short-run 관찰 가능.
- **판단**: 표 보고 막히면 멈추고 보고. 이상 없으면 본런.

### ②③④ 경험적 결과 — (A) 과적용 버전 (SUPERSEDED, 6000 step, host-GPU1, 2026-06-25) — 관찰만
> ⚠ 아래는 **정규화 과적용**(photo 5.6·nc 2.1) D4 의 사전점검 — **photo 열세를 드러내 정정의 근거**가 됨. corrected 결과는 (B) 참조.
비교=D(`gs_prior_full_dense`) 동일 step.

| 항 | D4 pre-check | D (동일 step) | 관찰 |
|---|---|---|---|
| ③ NaN/발산 | 없음 | 없음 | 안정 ✓ |
| ④ N @5000 | 3,016,213 | 2,973,844 | 미감소 ✓ |
| ④ 4907182 fp @500/5000 | 992 / **1012** | 992 / 992 | 회복동 미감소(소폭↑) ✓ |
| ③ loss/nc raw @6000 | **0.027** | 0.325 | D4 법선일관성 **12× tighter**(w_nc 2.1) |
| ③ loss/photo @5000 | **0.275** | 0.175 | D4 photo **+0.10(≈+57%)**, 전 step 일관 ⚠ |
| ③ psnr_train @5000/6000 | 12.5 / 13.2 | 17.6 / 16.9 | D4 **~3–5 낮음**(per-view 노이즈) ⚠ |

- **②(cp 발화)**: gate@15000 → 본 pre-check 미관찰. 본런 §3 모니터로 이관.
- **관찰 요지(판정=사람)**: 임계 게이트(③ 안정·④ 생성) 통과. **단 ③ photo 무열화는 미충족 쪽** — D4 photo 가 D 대비
  초반 일관되게 ~0.10↑(psnr ~3–5↓). 기전=nc 2.1 의 강한 법선 정규화(nc raw 12× tighter)가 기하를 묶어 photo 비용 발생.
  이는 D4 평탄화 목적과 **정합**(=정규화 발화)일 수도, nc 과제약으로 **최종 psnr 회귀**(무열화 위반)일 수도. 6000 step 으론 미분리.
  → 자동 본런의 "깨끗하면" 조건 **모호** → 멈추고 보고(판정=김휘영).

## §4 학습 (백그라운드, 2-GPU) — `run_d4.sh`
`gs_d4_dense`(host-GPU1)·`gs_d4_acmp`(host-GPU0) 병렬 각 ~4h → semantic TSDF(min-obs 3·voxel 0.05) →
GS-의미 분류(gssem) → Roofer(고정) → val3dity → facet/RMS/solid.
**모니터**: cp 압력이 후반에 약해지나(고정 ÷평균의 stale 조짐 = §7 running 전환 신호) 기록.

## §5 평가 (두 축 + 정성, 정본 matched 11 + survivor)
비교 = **D4 vs D vs v6 vs raw vs LiDAR vs ref**. 하니스 = `tum_mob_eval --classifier gssem` · `tum_mob_ref_rms`.
baseline 재사용: `eval_prior_full_gssem.json`(D) · `eval_v6_protect.json`(v6) · `eval_v6_raw.json`(raw/LiDAR) · `baselines.json`(ref).
- **A 품질(주)**: facet→ref/LiDAR · RMS→LiDAR · valid-solid. **평지붕 중심**.
- **B 생성(필수)**: assembled/8 · 회복동 밀도 — D 7/8 유지되나(깊이 낮춤에도).
- **C 무열화**: 제어 4906972·4908023 가 D보다 안 나빠지나.
- **D 전역정렬 watch**: 통째로 기운 채 조립되는 동 있나.
- **E 정성**: 평지붕 4906972·4907182 · 곡면 4906969 에 [D4|D|LiDAR|ref] × [점군|조립모델(면별 색)] 그림 — 실제로 펴졌나 vs 진짜 구조까지 뭉갰나.
- **평지붕/곡면 분리**: 평지붕={4906972,4907182}, 곡면={4906969}, 나머지=규칙(참조 지붕 곡면 유무).

## §6 판정 기준 (사전등록 — **🔒 LOCKED 2026-06-25 by 김휘영, 결과 보기 전 확정**, 판정=김휘영)
> **LOCKED**: 본 §6·§7 은 본런 결과를 보기 전 잠갔다. 이후 수정 금지(골대이동 금지). 판정만 김휘영.
> baseline = 보고된 D(gssem): assembled **7/8**, valid-solid **4/8**, 과분할 mean|Δfacet| **~3.4–3.5**(ref 대비),
> RMS→ref **~2.8 m**(LiDAR 1.4 m 미수렴), PSNR **19.9**, 제어 RMS **+0.3–0.5 m** 악화. (초안 — 김휘영 확정/수정)

- **성공(full support)** — 아래 **전부**:
  1. **A 평지붕 펴짐**: 평지붕셋{4906972,4907182} 과분할 mean|Δfacet| 가 **D(~3.4) 보다 의미있게↓ 하며 ref 로 접근**
     AND 평지붕 RMS→LiDAR 가 **D 대비 개선**(LiDAR 1.4 m 쪽으로). (= cp 가 실제로 발화·평면화)
  2. **B 생성 유지**: assembled **≥7/8** AND 회복동(4907182 등) 밀도 **D 대비 미감소**.
  3. **C 무열화**: 제어{4906972,4908023} RMS·과분할 **D보다 악화 없음**(노이즈 폭 내).
  4. **D 무-기움**: 전역으로 기운 채 조립되는 동 **신규 없음**.
  5. **E 정성**: 평지붕이 **눈에 띄게 펴지되** 곡면 4906969 가 **파국적 과-평탄(한 평면 뭉갬)** 아님.
- **부분지지(partial)**: cp 발화·평지붕 펴짐(A↑)은 보이나 **대가 동반** — 생성 슬립(<7/8 또는 회복동 밀도↓) OR 제어 악화(C)
  OR 곡면 4906969 과-평탄(E) 중 하나 이상. → **메커니즘은 작동, 트레이드오프 미해결 → §7 사다리로 이행**.
- **kill**: **§7 cp 사다리(세게→front-load→G2) 전부 소진 후에도** cp 미발화 — 평지붕 RMS→LiDAR ≈ D, 과분할 미감소 —
  즉 **GS 지붕 굴곡의 레버가 손실 균형이 아님**. kill 은 **D4 한 번이 아니라 사다리 뒤**.

## §7 cp 사다리 (D4-base 에서 cp 미발화 시 단계적 강화)
1. **세게(harder cp)**: cp share 1.0→1.5–2.0 (유효 cp 0.056→0.084–0.112) 로 평면화 압력↑. **과-평탄 위험 → 4906969 watch**.
2. **front-load(조기 게이트)**: `structure_warmup` 15000→~8000(또는 5000). 기하가 아직 말랑할 때 cp 작용(광도가 wavy 면에 over-commit 전).
3. **G2(그룹 정밀화)**: G2 merge 강화(`merge_d_tol`↓ / `merge_n_cos`↑ 또는 voxel↓) → 더 깨끗한 평면 그룹 → cp 타깃 개선,
   곡면 과병합 줄이며 발화. (running-stat 자동가중은 별건; scale 불균형엔 고정정규가 정답 — [[W_D4_precheck]] §2·3.)
> 각 단 1회 추가 학습(~4h/2arm), 모두 사전등록 §6 기준에 **그대로** 대입(골대이동 금지).

## §8 미결·주의 (open items — 김휘영 확인)
- **D 귀속 정정**: 보고된 D run = **w_structure 0.08(cp share 68%)**(`gs_prior_full_dense/versions.txt` 스탬프 확인). 사양의
  "D structure 86%" 는 **사전 감사 단계의 w_structure 0.3(cp 89%)** 수치 — 실제 baseline D 는 이미 0.08 로 부분 재조정된 상태.
  → D4 vs D 비교의 D 는 **68% 지배 런**(86% 아님). §3 표에 둘 다 명시. **실험 정의엔 무영향**(D4 는 항별 정규화로 well-defined).
- **w_photo 5.6 전역 스케일**: 모든 항을 share~1 로 올리면 total 절대크기↑ 이나 Adam 은 전역 상수에 거의 불변 → 상대 share 가 본질.
  단 ③ 안정에서 NaN/photo 무열화 실측 확인.
- **②(cp 발화) 사전관찰 한계**: 15k gate 로 short pre-check 직접관찰 불가 → 본런 모니터 + §7 로 처리(위 §3 ② 참조).
- **산출물**: `docs/experiments/w_d4/reports/W_D4.md`(두 축 표 + 정성 그림 + 본 사전등록 §6 대입) + G1_package. 한 커밋 "D4".
